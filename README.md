# practice-QNA

…or push an existing repository from the command line
1. git remote add origin Your_https of the git 
2. git branch -M main
3. git push -u origin main


### check process git user and email
1. git config user.name is not you just follow the steps
2. git init 
3. git config user.name "Your Name"  this is your name what you have to add in that
4. git config user.email "Your Email"  this is your email what you have to add in that 
5. this is the process to add your name and email in git config that will not affect your git config on other projects
6. check git config user.name once that is your is correct or not. 